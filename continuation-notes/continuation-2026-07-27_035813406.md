# 📋 Continuation Note - SWE-bench harness for small local LLMs (ornith) - 7/26/2026

## 🎯 Session Status Dashboard
**Project**: SWE-bench harness for small local LLMs (ornith)  
**Phase**: Attacking false-GREEN — compare2 (regression re-run) running overnight, compare3 (NEIGHBOR_INJECT A/B) queued behind it; A/B validity audited and confirmed clean  
**Completion**: 75%  
**Status**: In progress, healthy, UNATTENDED OVERNIGHT. Nothing to do until compare2 finishes. Mikey 2026-07-26 22:45 CDT: "I think you can just wait. This should take all night." NO GPU action pending — do NOT launch anything concurrent with the live run.  
**Last Update**: 2026-07-27T03:58:13.401Z

### ✅ Major Achievements This Session
- **Verified the compare2/compare3 A/B is methodologically clean BEFORE spending the GPU night on it — same frozen 18-instance set, scripts differ by exactly one line (output path), inject flag delivered via the waiter's env. NEIGHBOR_INJECT is the sole variable.**
- **Confirmed NO REGRESSION from this session's always-on changes at the 12/18 mark: 9 resolved vs baseline's 8 on the same 12, zero baseline-passers lost, single flip 15790 miss->PASS.**
- **Documented two data-shape gotchas that cost tool calls this session: results files are JSON lists not dicts, and diff's exit-1 renders as system_exec success:false.**

## 📚 Context Bridge for Next Session

### Enhanced System State
INFRA (unchanged): pop = 192.168.12.232, reached ONLY via mcp__remote-devices__system__system_exec -> ssh bard@192.168.12.232. Harness code /home/bard/Code/LLMOS; runs /home/bard/swe. ornith llama-server on 127.0.0.1:8080. Single GPU => runs serialize. Transfer cloud->pop via readable-python quoted heredoc, NEVER base64 (corrupts). Edits: whole-function rewrite or anchored insert, count-assert==1, .bak, py_compile; NEVER sed.

LIVE PROCESSES as of 2026-07-26 22:41 CDT:
 - run_ornith_compare2.py pid 314881 (started 19:31) -> runs/ornith/compare2.json. Gates OFF, TRUNC_RETRY on, current always-on code. This is the REGRESSION CHECK cell.
 - wait_compare3.sh pid 332937 -> polls count_compare2.py every 120s; when n>=18 it runs `NEIGHBOR_INJECT=1 python3 -u run_ornith_compare3.py` -> runs/ornith/compare3.json, logging to runs/ornith/compare3.log. This is the INJECT-ON cell.

COMPARE2 PROGRESS at 12/18 done, 9 resolved. Per-instance: astropy-12907 T, astropy-14995 T, django-15061 F, 15347 T, 15498 F, 15790 T, 15814 T, 15851 T, 15902 T, 16041 T, 16046 F, 16139 T. given_tests_regressed=[] on all completed. Restricted to those same 12, the 10/18 baseline (runs/ornith/compare1_baseline_jul25.json) had 8 resolved vs our 9 => NET +1, NO baseline-passer lost, single flip = 15790 miss->PASS. VERDICT SO FAR: this session's always-on changes caused NO REGRESSION.
REMAINING 6: 16255, 16379, 16527, 16595, 16873, 16910. Watch 16595 + 16873 (baseline passers — if either drops, that IS a regression to bisect). 16255/16379 are the known broke-neighbor false-GREENs NEIGHBOR_INJECT targets. 16910 is the hardest.

A/B VALIDITY — AUDITED THIS SESSION, CONFIRMED CLEAN (do not redo; full evidence in brain note ornith-compare2-compare3-ab-validated-2026-07-26):
 1. Both scripts read the SAME frozen runs/ornith/compare_set.json; confirmed = exactly the expected 18 ids. (compare3's docstring CLAIMS it rebuilds the set from ever-resolved history — misleading; it only builds when the file is absent. The file exists, so it reuses it.)
 2. `diff run_ornith_compare2.py run_ornith_compare3.py` = ONE hunk, line 30, OUT path only. Nothing else differs.
 3. NEIGHBOR_INJECT=1 is supplied by wait_compare3.sh's env, not by the script.
 => NEIGHBOR_INJECT is the ONLY variable. The eventual verdict is trustworthy.
GOTCHA: `diff` exiting 1 means "files differ", not failure; system_exec renders that as success:false. Don't misread it as an error.
GOTCHA 2: compare2.json/compare3.json are JSON *lists* of per-instance dicts (keys: id, resolved, given_tests_regressed, secs, ...), NOT dicts keyed by instance. Iterate the list; there is no .items().

UNCHANGED BIG PICTURE: dominant ornith failure is FALSE-GREEN, not convergence (declared-wrong 9 : never-converged 2; of the 9, 7 broke a neighbor, 2 didn't-fix). Root cause of undetected neighbors was _fault_proximity blind to django layout — FIXED, always-on, correct module now ranks #1. Prompt-only nudges do NOT move ornith (proven twice: ISOLATE, then the blast nudge); STRUCTURE does. Hence NEIGHBOR_INJECT (show, don't force): after a successful patch, phase_run auto-runs neighbor_tests and injects any regression into result['blast_radius']. Leak-safe (base-passing tests exclude FAIL_TO_PASS by construction).

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **FIRST: check whether compare2 finished — ssh bard@192.168.12.232 'python3 /home/bard/swe/count_compare2.py' and ps for pid 314881 / run_ornith_compare2.py.**
- **When compare2 hits 18: final resolved/18 vs the 10/18 baseline. >=10 => no regression, done. If <10, identify which baseline-passer dropped and bisect the always-on changes by re-running that single instance with TRUNC_RETRY=0 (proximity fix is not separately gated).**
- **When compare3 finishes: the real question. compare2(inject off) vs compare3(inject on), per instance. Did 16255/16379-type broke-neighbor false-GREENs convert to resolved? Did result['blast_radius'] actually appear in the compare3 records? Critically: when the agent SAW a regression, did it REPAIR the fix or REVERT it (reverting satisfies the neighbor but loses the real fix — that is the failure mode to look for). And are the passers unharmed?**
- **If inject works: consider making NEIGHBOR_INJECT default-on. If the agent reverts instead of repairs: fall back to the harness GATE (staged /tmp/patch_regress.py — now viable since proximity is fixed).**
- **Still open, untouched: the 2/9 didn't-fix false-GREENs (weak reproduction). _reproduction_strength exists but is advisory only; a pre-submit repro-strength gate is the next candidate lever. Design offline, test after this A/B clears the GPU.**
- **DEFERRED (Mikey's idea, values call — keep env-gated and OFF for measurement): web-searching the reproduction error + repo. It works (finds django tickets #34088/#34209) but on these HISTORICAL instances it retrieves the gold fix, inflating the benchmark and no longer measuring reasoning. Legitimate for a real agent on novel bugs.**

### Critical Success Factors
- Only canonical hidden tests decide pass/fail; runtime NEVER reads gold/FAIL_TO_PASS during a run. Leak rule is absolute.
- pop is ALWAYS 192.168.12.232. Single GPU — NEVER launch a run concurrent with a live one. Two runs are already scheduled; adding a third corrupts both.
- Do not touch the harness while compare2/compare3 are live — an edit mid-run silently changes the cell being measured and destroys the A/B.
- All harness changes env-gated + count-asserted + .bak + py_compile; with flags off, behavior must remain byte-identical to pre-session.
- Measure, don't pattern-match. Be honest about mistakes — the '2/21' over-report and the disproven probe-gate are the standing reminders.
- Per pref-llmos-owns-problem-handling-2026-07-04: LLMOS owns problem-handling; don't race it with concurrent writes to shared substrate.

### Testing/Validation Criteria
- [ ] compare2 final: resolved/18 >= 10 => no regression from this session's always-on changes
- [ ] compare3 (inject ON) vs compare2 (OFF): broke-neighbor instances convert miss->PASS, or at minimum result['blast_radius'] is present and the agent acted on it
- [ ] Agent REPAIRED rather than REVERTED when shown a regression (revert = lever fails its purpose)
- [ ] Passers unharmed in compare3 relative to compare2
- [ ] off (all flags unset) remains byte-identical to pre-session behavior
- [ ] No leak: FAIL_TO_PASS never runs inside the agent loop

## 🎯 Session Continuity

**BREAKTHROUGH**: SWE-bench harness for small local LLMs (ornith) session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: SWE-bench harness for small local LLMs (ornith)
3. **Continue from**: Attacking false-GREEN — compare2 (regression re-run) running overnight, compare3 (NEIGHBOR_INJECT A/B) queued behind it; A/B validity audited and confirmed clean
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly