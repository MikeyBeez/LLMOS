# 📋 Continuation Note - LLMOS SWE-bench full-300 run - 7/12/2026

## 🎯 Session Status Dashboard
**Project**: LLMOS SWE-bench full-300 run  
**Phase**: Full SWE-bench Lite run (paused at 54/300) + pending Opus comparison decision  
**Completion**: 18%  
**Status**: PAUSED by Mikey — runner killed, monitor disabled. Fully resumable, nothing lost.  
**Last Update**: 2026-07-12T18:50:05.666Z

### ✅ Major Achievements This Session
- **Full-300 SWE-bench Lite run built (resumable runner, interleaved repos) and executed to 54/300, 21 resolved (38%), env_ok ~100%**
- **Learning loop closed and validated: seaborn-3407 resolved with gold-patch-identical fix after pattern injection (67% pilot)**
- **Engineering-patterns store promoted system-wide (git-tracked, protocol written, injected into fix phase)**
- **Autonomous monitor (30-min scheduled task) made 3 correct harness interventions with commits mirrored to both machines**
- **Outage audit: two invisibly-tainted results found, removed, re-run fairly; network preflight gate added per Mikey's directive**
- **FIX_BUDGET 40→80 (pylint resolved as direct result); mirror clone timeout 1800→7200 (sklearn unblocked)**

## 📚 Context Bridge for Next Session

### Enhanced System State
RUN STATE: Full 300-instance SWE-bench Lite run on pop-os (ssh alias pop-os, user bard). Runner: ~/swe/run_full300.py (RESUMABLE — skips ids already in ~/swe/results_full300.json; relaunch with `nohup python3 ~/swe/run_full300.py > ~/swe/run_full300.log 2>&1 &`). Currently STOPPED at 54 done, 21 resolved (38%). Monitor: Cowork scheduled task "swebench-full300-monitor" (every 30 min) currently DISABLED — re-enable via update_scheduled_task enabled=true when resuming. Monitor journal + queued improvements: pop-os ~/swe/monitor_state.json. llama-server (ornith_35b, port 8080) still running on pop-os. CRITICAL PKILL FOOTGUN: `pkill -f "[r]un_full300"` in its own ssh call with NO other occurrence of the plain string in the command line, else it kills the ssh shell itself.

WEEK ARC (model weights never changed): 0% (lying scorer) → 44% (env work, pilot 9) → 56% (FIX_BUDGET 40→80, commit 48a6bfc) → 67% pilot (patterns store) → held-out full run settling ~38-48%. env_ok essentially 100% throughout — environment problem is solved; fix-phase judgment is the frontier.

KEY MACHINERY BUILT THIS SESSION: (1) engineering-patterns store: master ~/Code/LLMOS/engineering-patterns.json (git, both machines, commits fd69129+1b26779), merged with runtime ~/swe/patterns.json by trace_consumers.patterns_load(), injected into phase-2 fix goal. Protocol written: /Users/bard/Code/mcp-protocols/protocols/engineering-patterns.md. Rule: pattern-level knowledge only, NEVER instance-specific (answer leakage). Seaborn-3407 resolved via patterns (gold-patch-identical fix, np.array-of-tuples removal) — learning loop validated. (2) Network preflight gate in run_full300.py (pause loudly until pypi.org:443 reachable) — Mikey's directive: no network, no benchmarking. (3) Monitor made 3+ autonomous interventions: scorer test-label resolution fix (4051a2a pop-os / 5de0ea7 Mac), h_locate fix (9b9a72c / ab689f2), smoke-guard same-file over-rejection fix (bcf4acc). (4) Outage audit: internet outage during "cold stretch" tainted requests-2674 (guard bug) and xarray-4493 (pypi failures, numpy downgrade); both removed+re-run fairly (both honest misses now). Backups: results_full300.json.bak-*.

PENDING DECISIONS: (a) Monitor's sklearn skip-fix recommendation: bootstrap installs repo-declared test extras + scorer flags all-F2P-skipped outcomes (sklearn-25570 was graded miss on "3 skipped"). Mikey not yet confirmed; I endorsed a+b. Apply at next natural stop. (b) Queued in monitor_state.json: net_suspect taint flag (mid-instance network drop detection on raw stderr, auto-quarantine). (c) OPUS SWITCH (Mikey requested, then paused): plan = LiteLLM proxy on pop-os translating OpenAI /v1/chat/completions → Anthropic API; harness unchanged except host/model (claude-opus-4-8). Needs ANTHROPIC_API_KEY on pop-os (none present). Recommended pilot-9 comparison first (~$50-150) vs full 300 ($1500-4500). Purpose: same harness, vary model — splits system-vs-model credit. Mikey also wants THIS CHAT on Opus (app model picker) — that is why this new session exists.

MIKEY'S GOVERNING PRINCIPLES (established this session, respect them): build a better system, not a better score; benchmark measures the SYSTEM not the model; no frozen agent — any harness change allowed SHORT OF GIVING THE ANSWER (no gold_patch/test_patch content near prompts/patterns/remedies, no bug-layer web search — env-layer search is fine); count learning-loop results, report provenance transparently (seaborn counted, footnoted); skip already-resolved instances (carried entries tagged "carried"); if network is out, STOP the run.

RESULTS PROVENANCE: results_full300.json entries: "carried":"pilot-2026-07-11" = pilot resolves carried forward; "rerun" tags on pilot-era retries in results_v2.json (pilot file, tally 6/9). Traces: ~/swe/traces_v2/ (prior attempts in prior_40turn/, prior_80turn/). Brain keys: llmos-swebench-v4-pilot-final, llmos-swebench-80turn-rerun, llmos-patterns-store-first-win, engineering-patterns-store, swebench-large-repo-clone-timeout, python-bug-pattern-multiindex-identity.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **If resuming the run: relaunch runner on pop-os, re-enable swebench-full300-monitor scheduled task**
- **Decide monitor's sklearn skip-fix (install test extras + flag all-skipped scoring) — recommended yes**
- **Opus comparison: get ANTHROPIC_API_KEY onto pop-os, stand up LiteLLM proxy, run pilot-9 with claude-opus-4-8, compare against ornith 67%**
- **Let monitor apply queued net_suspect taint flag at next natural stop**
- **Eventually: write up the week (envelope: 16GB, model-over-VRAM, env-from-scratch, learning loop closed; the harness-was-the-variable story 0→44→56→67)**

### Critical Success Factors
- Answer-leakage line is absolute: nothing from gold_patch/test_patch/FAIL_TO_PASS reaches the model; pattern-level knowledge only
- results_full300.json is the single source of truth; runner clobbers external edits made while it runs — kill first, edit, relaunch
- pkill footgun: bracket the pattern and keep plain string out of the same command line
- Monitor auto-restarts a dead runner when enabled — disable it BEFORE intentionally stopping the run
- Mikey wants concision; timestamps on responses; he checks in with 'what is the status, please'

## 🎯 Session Continuity

**BREAKTHROUGH**: LLMOS SWE-bench full-300 run session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: LLMOS SWE-bench full-300 run
3. **Continue from**: Full SWE-bench Lite run (paused at 54/300) + pending Opus comparison decision
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly