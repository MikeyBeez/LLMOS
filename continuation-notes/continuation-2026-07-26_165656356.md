# 📋 Continuation Note - SWE-bench harness for small local LLMs (ornith) - 7/26/2026

## 🎯 Session Status Dashboard
**Project**: SWE-bench harness for small local LLMs (ornith)  
**Phase**: Harness reliability — attacking false-GREEN (verification validity)  
**Completion**: 55%  
**Status**: In progress. 3 fixes shipped+tested; stuck-breaker live A/B running; regression-gate fix DESIGNED but NOT applied pending a design decision (heuristic baseline vs real PASS_TO_PASS).  
**Last Update**: 2026-07-26T16:56:56.355Z

### ✅ Major Achievements This Session
- **Diagnosed false-GREEN as the dominant failure (75% of declared-done runs wrong; ratio 9 declared-wrong : 2 never-converged), reframing the effort from convergence to verification validity**
- **Decomposed the 9 false-GREENs: 7 broke-a-neighbor (P2P regression), 2 didn't-fix (F2P failed)**
- **Shipped STUCK_ESCALATE stuck-breaker (swe_fix_tools.py); 5 unit tests + offline replay (fires only on the 2 flails, 0 collateral)**
- **Shipped TRUNC_RETRY adaptive anti-truncation (tool_call_cpu.py _chat rewrite); 4 unit tests**
- **Added finish_reason/trunc_grow to generation emit for the Live monitor**
- **Launched stuck-breaker live A/B (run_ab_stuck.sh) varying STUCK_ESCALATE, ISOLATE off, TRUNC_RETRY on both arms**
- **Established P2P=6 / F2P=1-2 tests per instance**

## 📚 Context Bridge for Next Session

### Enhanced System State
INFRA: pop = 192.168.12.232, reached ONLY via mcp__remote-devices__system__system_exec -> ssh bard@192.168.12.232 (no direct ssh from cloud). Harness code /home/bard/Code/LLMOS on pop; runs /home/bard/swe. ornith llama-server pid ~178075 on 127.0.0.1:8080. File transfer to pop: pipe base64 through ssh, `base64 -d` on pop, then a python ast.parse / bash -n integrity check (transfers corrupt intermittently — always verify). Editing style Mikey requires: whole-function rewrite or anchored insert with count-assert (assert anchor appears exactly once), NEVER sed; every patcher makes a .bak-* backup and py_compile checks.

KEY FINDING THIS SESSION (the strategic pivot): FALSE-GREEN. Cross-tabbed how each run ended vs whether it passed the canonical hidden tests across 14 A/B runs: 12 DECLARED done, only 3 actually correct => ~75% false-GREEN on this (hard, django-only) sample. Robust part is the RATIO: declared-but-wrong (9) >> never-converged (2). Bottleneck is NOT convergence (patch-thrash/truncation) — it's the agent not knowing when it's right. Decomposition of the 9 (score tails in ~/swe/runs/ab/ab.log): 7 = BROKE A NEIGHBOR (F2P passed but regressed a PASS_TO_PASS test); 2 = DIDN'T FIX (F2P failed). Harness given-tests check rated all 7 '0 regressed' because _capture_baseline samples 1 test/file across 6 files (hints from reproduction traceback), missing the broken test.

ARCHITECTURE: run_one -> phase_run(fix) ends when agent declares done; ONLY submit gate (_fix_gate, swe_agent_v2.py) is _diff_nonempty. score() (~line 748) runs canonical recipe post-hoc (model diff + inst['test_patch'], FAIL_TO_PASS then PASS_TO_PASS); 'resolved' saved to trace but NEVER fed to agent (F2P feedback = leak = forbidden). Wrong stop is final.

NEW DATA: F2P=1-2 tests/instance; P2P=6 tests/instance. Canonical P2P ~= same size as our 6-sample baseline — cost was never the blocker; the F2P leak rule is. Work dirs+envs survive under ~/swe/work/ (41G) for timing/validation.

PENDING DECISION (Mikey asked 'why not just run the canonical ones'): regression guard = (a) HEURISTIC enrich-our-baseline (no leak, may miss) vs (b) REAL PASS_TO_PASS 6 tests during run (precise, catches all 7, ~same cost) but MILD leak (P2P ids reveal watched files). FAIL_TO_PASS is a HARD no during the run. Need his call before applying.

STAGED BUT NOT APPLIED: /tmp/patch_regress.py = HEURISTIC version (REGRESS_GATE env; 3 count-asserted edits: enrich _capture_baseline, batch _check_regressions with tail, bounce _fix_gate on regression). base64 of it is in the transcript.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **DECIDE with Mikey: regression guard heuristic vs real PASS_TO_PASS (he is leaning toward running the canonical tests)**
- **Apply chosen regression-gate; if canonical-P2P, revise to read inst['PASS_TO_PASS'] at gate time instead of the heuristic baseline**
- **Validate after the stuck A/B finishes: re-run broke-neighbor instances (16255, 16379) REGRESS_GATE off vs on; measure false-GREEN drop; watch for agent reverting its real fix to satisfy the neighbor**
- **Then tackle the 2 didn't-fix false-GREENs: strengthen reproduction before submit (_reproduction_strength exists, advisory only)**
- **Report stuck-breaker A/B verdict (scheduled check trig_011tUKJdZAzY1Y9Pv68CHhCj ~16:45 UTC)**

### Critical Success Factors
- Only canonical hidden tests decide pass/fail; runtime NEVER reads gold/FAIL_TO_PASS during a run (absolute)
- pop is ALWAYS 192.168.12.232
- All harness changes env-gated + count-asserted + backup + py_compile; validate before any live A/B
- Single GPU (llama-server --parallel 1): live runs serialize — don't run a validation A/B concurrent with the stuck A/B
- Measure, don't pattern-match (the probe-gate I nearly built earlier was disproven by measurement)

### Testing/Validation Criteria
- [ ] Regression gate: on the 7 broke-neighbor cells, REGRESS_GATE=1 converts declared-but-wrong into resolved or a within-run bounce, without hurting the 3 true wins
- [ ] Off (REGRESS_GATE unset) is byte-identical to current behavior
- [ ] No leak: FAIL_TO_PASS never runs during the agent loop

## 🎯 Session Continuity

**BREAKTHROUGH**: SWE-bench harness for small local LLMs (ornith) session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: SWE-bench harness for small local LLMs (ornith)
3. **Continue from**: Harness reliability — attacking false-GREEN (verification validity)
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly