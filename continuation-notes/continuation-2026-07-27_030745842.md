# 📋 Continuation Note - SWE-bench harness for small local LLMs (ornith) - 7/26/2026

## 🎯 Session Status Dashboard
**Project**: SWE-bench harness for small local LLMs (ornith)  
**Phase**: Attacking false-GREEN (broke-neighbor regressions) — fixes built, inject A/B + regression re-run in flight  
**Completion**: 70%  
**Status**: In progress, healthy. No regression from this session's changes (re-run tracking ABOVE the 10/18 baseline). Two runs live on pop; inject is the chosen lever and is queued.  
**Last Update**: 2026-07-27T03:07:45.835Z

### ✅ Major Achievements This Session
- **Diagnosed false-GREEN as the dominant failure (ratio 9 broke-neighbor+didn't-fix : 2 never-converged), reframing the whole effort from convergence to verification validity**
- **Root-caused the missed regressions to a proximity function blind to django layout; fixed it (correct test module #1, was random) with zero answer-key data**
- **Proved (A/B) that prompt nudges don't move ornith — twice (ISOLATE, then the blast nudge); structure does**
- **Shipped 5 env-gated fixes, all count-asserted + suite green (33 tests): proximity, TRUNC_RETRY, stuck-breaker, neighbor_tests, NEIGHBOR_INJECT**
- **Corrected a scary but wrong 2/21 framing — real baseline is 10/18; confirmed NO regression via an in-flight apples-to-apples re-run (running above baseline, 15790 improved)**
- **Found the reliable cloud->pop transfer method (readable python via quoted heredoc; base64 corrupts)**

## 📚 Context Bridge for Next Session

### Enhanced System State
INFRA: pop = 192.168.12.232, reached ONLY via mcp__remote-devices__system__system_exec -> ssh bard@192.168.12.232. Harness code /home/bard/Code/LLMOS; runs /home/bard/swe. ornith llama-server on 127.0.0.1:8080. FILE TRANSFER LESSON (important): base64 blobs piped in kept getting corrupted in transit (stray non-ASCII); a sha256 check catches it. The RELIABLE method is sending readable Python via a quoted heredoc: `ssh bard@pop 'cat > /tmp/x.py' <<'EOF' ... EOF`, and let the script self-verify (count-asserts + py_compile abort before writing). Editing style: whole-function rewrite or anchored insert with count-assert==1, .bak backup, py_compile; NEVER sed.

BIG PICTURE FINDING: the dominant failure for ornith on SWE-bench is FALSE-GREEN, not convergence. Of 14 A/B runs, 12 declared done but only 3 were actually correct (~75% false-GREEN on a hard django subset; robust part is the ratio, declared-wrong 9 : never-converged 2). Decomposition of the 9: 7 = BROKE A NEIGHBOR (fixed the target, F2P passes, but regressed an existing PASS_TO_PASS test); 2 = didn't-fix (F2P failed).

ROOT CAUSE of the broke-neighbors going undetected: _fault_proximity (swe_fix_tools.py) scored the correct django test module 0 (source under django/..., tests under tests/<app>_tests/... share no path prefix; it matched on __init__), so the harness sampled ~6 random tests and caught 0 of 7 regressions. FIXED: name-aware _fault_proximity (token overlap + exact app-dir match) — correct module now ranks #1 (was #16/#67). This is ALWAYS-ON.

FIXES SHIPPED this session (all env-gated unless noted, count-asserted, .bak backups, suite green = 33 tests):
 - name-aware _fault_proximity (ALWAYS ON) — the root-cause fix.
 - TRUNC_RETRY (default ON): tool_call_cpu._chat re-generates with doubled max_tokens on finish_reason==length. Fires live (8x on the 16910 marquee alone). Real work.
 - STUCK_ESCALATE (default off): patch-thrash breaker. VERDICT: inert on the standard harness (stuck_fires=0 across all 13 A/B cells) — the 27-35-patch flails were an ISOLATE_DISCIPLINE artifact, not intrinsic. Keep as cheap backstop only.
 - BLAST_RADIUS (default off): neighbor_tests tool + prompt nudge + baseline enrichment (full nearest module). A/B VERDICT: nudge FAILS — ornith called neighbor_tests once in 4 on-runs and shipped false-GREEN anyway. Prompt-only doesn't move this model (2nd time; ISOLATE was the 1st).
 - NEIGHBOR_INJECT (default off): THE CHOSEN LEVER. After a successful patch, phase_run auto-runs neighbor_tests and INJECTS any regression into the agent's result (result['blast_radius']) — show, don't force. Reuses h_neighbor_tests (leak-safe: base-passing tests only). Enrichment gate widened to fire under BLAST_RADIUS OR NEIGHBOR_INJECT.

FRAMING CORRECTION (Mikey flagged 'we did much better before' — he was right): the '2/21' I'd been reporting was only the 4 HARDEST instances (16046/16255/16379/16910), ALL already misses in the pre-change 10/18 baseline. Real pre-change rate = 10/18 (56%), saved as runs/ornith/compare1_baseline_jul25.json. 16046 even improved this session.

LIVE NOW on pop (single GPU, serialized):
 - compare2 (run_ornith_compare2.py -> runs/ornith/compare2.json): the 18-instance regression re-run on CURRENT code, gates off, TRUNC_RETRY on. At 11/18, 8 resolved, only flip vs baseline is 15790 miss->PASS. NO REGRESSION; running above baseline pace.
 - compare3 (queued: wait_compare3.sh pid ~332937 -> waits for compare2 to hit 18, then runs run_ornith_compare3.py with NEIGHBOR_INJECT=1 -> runs/ornith/compare3.json). So compare2(inject off) vs compare3(inject on) = the A/B of the chosen fix on the full 18.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **When compare2 finishes: confirm final resolved/18 vs 10/18 baseline (regression check). If <10, bisect always-on changes (re-run a flipped instance with TRUNC_RETRY=0).**
- **When compare3 finishes: compare2(off) vs compare3(NEIGHBOR_INJECT on) — does injecting the regression convert broke-neighbor false-GREENs (16255/16379-type) into resolved, without hurting the passers? Check result['blast_radius'] appeared and whether the agent repaired vs reverted its fix.**
- **If inject works: consider making NEIGHBOR_INJECT default-on. If it doesn't (agent reverts its real fix to satisfy the neighbor), fall back to the harness GATE (staged /tmp/patch_regress.py, now works since proximity is fixed).**
- **Still open: the 2/9 didn't-fix false-GREENs (weak reproduction) — strengthen the repro before submit (_reproduction_strength exists, advisory only).**
- **DEFERRED (Mikey's idea, values call): search the reproduction error + repo on the web. Works (finds the exact django tickets #34088/#34209) — but on these HISTORICAL instances that retrieves the gold fix, so it inflates the benchmark / stops measuring reasoning. Legitimate for a real agent on novel bugs; keep env-gated + off for measurement.**

### Critical Success Factors
- Only canonical hidden tests decide pass/fail; runtime NEVER reads gold/FAIL_TO_PASS during a run (leak rule absolute). neighbor_tests is leak-safe because base-passing tests exclude FAIL_TO_PASS by construction.
- pop is ALWAYS 192.168.12.232; single GPU so live runs serialize — never launch a run concurrent with another.
- All harness changes env-gated + count-asserted + .bak + py_compile; off = byte-identical. Validate (suite + offline) before committing GPU.
- Transfer cloud->pop via readable-python heredoc, not base64 (corrupts).
- Measure, don't pattern-match; be honest about mistakes (I over-reported 2/21 without context; the probe-gate I nearly built earlier was disproven by measurement).

### Testing/Validation Criteria
- [ ] compare2 final: resolved/18 >= 10 => no regression from this session's always-on changes
- [ ] compare3 (inject on) vs compare2 (off): broke-neighbor instances convert miss->PASS, or at least the agent SEES result['blast_radius'] and acts; passers unharmed
- [ ] off (all flags unset) remains byte-identical to pre-session behavior
- [ ] no leak: FAIL_TO_PASS never runs inside the agent loop

## 🎯 Session Continuity

**BREAKTHROUGH**: SWE-bench harness for small local LLMs (ornith) session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: SWE-bench harness for small local LLMs (ornith)
3. **Continue from**: Attacking false-GREEN (broke-neighbor regressions) — fixes built, inject A/B + regression re-run in flight
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly