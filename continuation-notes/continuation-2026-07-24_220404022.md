# 📋 Continuation Note - LLMOS-SWE-workshop - 7/24/2026

## 🎯 Session Status Dashboard
**Project**: LLMOS-SWE-workshop  
**Phase**: Laguna tail/complementarity test + Qwen3.6 evaluation. pop-os powered down 2026-07-24 ~17:00 CDT to move to ethernet.  
**Completion**: 46%  
**Status**: POP-OS IS OFF (physical move to wired network). Tail run 1/20 saved and resumable. Qwen3.6 GGUF download 419MB of 18.2GB, resumable. THREE prior conclusions RETRACTED this session - read those first.  
**Last Update**: 2026-07-24T22:04:03.978Z

### ✅ Major Achievements This Session
- **DJANGO MISS PATTERN = LOCALIZATION, not semantics. All 6 regress1 misses are competent Django patches in the wrong place. Specimen: 15814 wrote concrete_model (the right proxy-model insight) into django/db/models/query.py instead of django/db/models/sql/query.py - it went to the same-basename file one directory up. Mirror image: 16910 landed the RIGHT file and then emitted a structurally corrupt patch. Checked and REJECTED the obvious confound: single-hunk patches are NOT predictive (8 of 12 resolves are also one hunk, one file; patch sizes overlap 432-1444B resolved vs 524-695B miss).**
- **HARNESS BUG FOUND (not yet fixed): swe_agent_v2.py line 1142 syntax_ok = f_state.get('syntax_ok', True) DEFAULTS TO TRUE. django-16910's corrupt patch (duplicated def at column 0, invented 'except self.model._meta.get_field(field_name).__class__:', ate the next function's docstring) was recorded syntax_ok True, which bought it +4 in form_rank and won attempt selection. Fix = real ast.parse of the post-apply file plus an import smoke test. Cheapest win available.**
- **CONTEXT ANALYSIS written to ~/swe/ctx_analysis.py. Peak ctx: max 91636, p90 79540, median 53480. 4 of 18 exceed 65536 - and all 4 are MISSES (15814, 16910, 15498, 16379). So do NOT halve context for --parallel 2; use --parallel 2 --ctx-size 262144 and let KV memory decide. Mikey explicitly agreed not to halve.**
- **DFLASH CLOSED NEGATIVE for the third and correct reason. poolside/Laguna-XS-2.1-DFlash DOES exist (462M params, safetensors, downloaded to ~/models/dflash/xs21/). Yesterday's 'poolside ships S-only drafts' was WRONG. The real blocker, found by running the conversion: convert_hf_to_gguf.py errors 'Model DFlashLagunaForCausalLM is not supported'. llama.cpp HAS generic DFlash/EAGLE3 support (src/models/dflash.cpp, --target-model-dir) but this architecture has no converter class registered. Fixing needs code, not flags.**
- **LAGUNA-XS-2.1 REAL CONFIG: 40 layers, hidden 2048, 256 experts, top-8 per token, moe_intermediate_size 512, max_position 262144. It is ALSO an ~A3B-class MoE - Qwen3.6-35B-A3B is the SAME architectural weight class, not a lighter model. Do not expect a free speed win from switching.**
- **GPU OCCUPANCY unresolved but bounded. Model says 10.5 percent busy; live sampling during generation says mean 38.1 percent util, non-zero 94.5 percent of samples. The sampling window only caught agent-loop time, not pip/pytest. Long sampler output is at /tmp/gpu_long.txt (lost on reboot).**

## 📚 Context Bridge for Next Session

### Enhanced System State
This session was a chain of falsified conclusions; the retractions matter more than the new work. (1) The morning claim "the tuning cascade gave 3.5x wall-clock" is WRONG. llama-server pid 3164 started 14:25:26 and the box booted 14:23:34, but the fast instances completed BEFORE both events (16255 at 12:08, 16379 at 13:51, 16527 at 14:04). Only 16873 and 16910 ran on the tuned server. Independent arithmetic agrees: eval-token volume is ~16.3k mean on BOTH sides of the split, and decode 35.95 to 69.39 t/s explains only ~217s of a ~1965s median gap. The cause of the 12-to-13 discontinuity is STILL UNIDENTIFIED; leading candidate is uv env cache warming after 10 prior django instances. (2) The follow-on theory that prompt rewriting was invalidating the KV cache is FALSE. /slots shows n_prompt_tokens 29369, cache 28661, processed 638 = 97.6 percent prefix hit rate. Prefix caching works fine. (3) Nearly reported the atlas as answer leakage; it is CLEAN. _load_atlas(repo, exclude_iid=inst["instance_id"]) at swe_agent_v2.py line 1069 does real leave-one-out from ledger.jsonl. The atlas/*.md files are regenerated artifacts that LOOK like cheat sheets but are not the injection path - anyone reading that directory will draw the same wrong conclusion. METHOD LESSON, the most valuable output of the day: check lstart/uptime/mtime BEFORE attributing a performance discontinuity to a config change. Inferring cause from the shape of a metric produced two wrong causal stories in one session. Separately, Claude's own response timestamps drifted ~45 min ahead of real time by extrapolating instead of running date - always check.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **ON BOOT: relaunch the server FIRST - bash ~/models/laguna-xs2/run_laguna.sh (single source of truth, all measured numbers in comments). Nothing works until this is up. Verify: curl -sf localhost:8080/health**
- **RESUME TAIL RUN: cd ~/swe && NUM_PREDICT=4096 nohup python3 -u run_tail.py > runs/laguna/tail1.log 2>&1 &  -- 20 never-resolved instances, 1 saved (astropy-14182 miss 687s), 19 to go. NEVER pass --rebuild (it would rebuild the set from ever-resolved and destroy tail_set.json). This is the decisive switch/no-switch experiment.**
- **RESUME QWEN DOWNLOAD on the new wired link: python3 -c "from huggingface_hub import hf_hub_download; hf_hub_download('unsloth/Qwen3.6-35B-A3B-MTP-GGUF','Qwen3.6-35B-A3B-UD-IQ4_XS.gguf', local_dir='/home/bard/models/qwen36')" -- resumes from .incomplete. 419MB of 18.2GB done. Consider putting it on /mnt/data (7.3T, 89 percent empty) instead of the 449G NVMe.**
- **WHY QWEN: unsloth/Qwen3.6-35B-A3B-MTP-GGUF ships multi-token prediction (native speculative decoding) as prebuilt GGUF - exactly what DFlash was meant to provide - plus 14 Unsloth Dynamic per-layer mixed-precision quants and an imatrix. UD-IQ4_XS is 18.2GB vs Laguna Q4_K_M 20.3GB, so 2.1GB more room for resident experts. Apache-2.0. Caveat: Qwen3.6 is multimodal (mmproj files) so it carries unused vision params.**
- **FOUR LEGITIMATE (non-leaking) LOCALIZATION AIDS, cheapest first: (1) fix the syntax_ok default-True bug; (2) basename-collision guard - when the model names a file, surface other files with the same basename (this exact confusion killed 15814); (3) second-site sweep - after a patch is drafted, grep the repo at base commit for other untouched occurrences of the changed pattern and ask 'is your fix complete?' (targets sympy-20639-class two-site fixes); (4) upgrade the atlas ledger from file-level to symbol-level (file::function), same LOO, same provenance as the file names already stored.**
- **PARKED: --parallel 2 throughput test (use ctx-size 262144, NOT a 65k halving); Claude Code vs LLMOS same-model experiment; MOE_TRACE_OUT expert-skew measurement.**

### Critical Success Factors
- ANSWER LEAKAGE ABSOLUTE; runtime never reads ~/swe/research/ or gold/test patches. The atlas is fine (verified LOO at the call site).
- pkill SELF-MATCH TRAP, new variant learned the hard way: the bracket form [r]un_x.py is NOT enough if the SAME ssh command line contains the plain literal elsewhere (e.g. in a pgrep -af). It killed the wrapper shell and returned exit 255. Prefer kill -9 <PID>.
- ssh+MCP calls cap ~60-70s; background long ops with nohup+log and poll. NO heredocs over ssh - transfer scripts by piping base64 -d into ssh 'cat > file', which worked well.
- run_tail.py and run_passing.py are resumable; a kill loses only the in-flight instance.
- 31GB RAM is the real constraint for --parallel 2, not VRAM: 13Gi used / 17Gi available with llama-server holding ~8GB of CPU-resident experts plus pytest processes.
- pop-os network was a USB WiFi adapter (wlx74da388c1c62) at ~928 KB/s total host RX - that is why an 18.2GB download stalled. The whole point of the move is to fix this.
- Disks are NOT full: 189G free on / and 6.2T free on /mnt/data; inodes 9 percent and 1 percent.

### Testing/Validation Criteria
- [ ] Tail run: len(tail1.json) grows toward 20. ANY resolved instance is complementarity evidence for Laguna; zero resolved across 20 is a strong no-switch signal.
- [ ] Server health after reboot: curl -sf localhost:8080/health, nvidia-smi ~11.9GB idle, dmidecode should still show 3200 MT/s on both channels.
- [ ] Qwen download integrity: final file should be exactly 18209036576 bytes.
- [ ] Re-measure download throughput on ethernet before assuming the stall is gone.

## 🎯 Session Continuity

**BREAKTHROUGH**: LLMOS-SWE-workshop session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: LLMOS-SWE-workshop
3. **Continue from**: Laguna tail/complementarity test + Qwen3.6 evaluation. pop-os powered down 2026-07-24 ~17:00 CDT to move to ethernet.
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly