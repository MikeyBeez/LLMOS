# 📋 Continuation Note - llmos - 7/18/2026

## 🎯 Session Status Dashboard
**Project**: llmos  
**Phase**: WORKSHOP MODE — iterate failure batches until each cracks; full-300 deferred until workshop done  
**Completion**: 55%  
**Status**: In progress — fix4 rerun finishing (2/4 graduated); workshop iteration 1 next  
**Last Update**: 2026-07-19T04:39:25.623Z

### ✅ Major Achievements This Session
- **Workshop protocol established + driver built (workshop.py, failset/graduated tracking)**
- **Research-zone doctrine: research_ledger.py + fence audit; 4 seed records (flask-5063 format, 3362 progress, 11148 theory, 17087 gate-calibration) each tagged with which of the three questions failed + rule candidate**
- **fix4 harness (v3.1): probe retries, best-by-form TTS selection, reproduce cap, tail hygiene -- converted 3362 and 5131 from 0-for-2 to first-attempt solves**
- **WORKSHEET shipped (v3.2): deterministic state object re-shown every turn (Mikey: if it can't hold onto it, write it down)**
- **symmap schema layer (reformatted map: object->fields from repo's own usage) + format_job (legitimate-signals label ranking) -- both correctly derive Subdomain for flask-5063 without leakage**
- **Caught format_job reading applied hidden test patch within the hour (too-good signal); leakage guard: base-commit reads only**
- **THE THREE QUESTIONS canonized as pattern #35; 35 patterns total**
- **Paper updated with containers section + env-building asymmetry (our benchmark does more)**

## 📚 Context Bridge for Next Session

### Enhanced System State
GOVERNING PROTOCOL (Mikey, 2026-07-19): workshop mode. Work failures in batches of ~4: run, analyze each failure with THE THREE QUESTIONS (1 what were we trying to get? 2 can we get it? 3 if we got it, is it in the right format?), make GENERAL harness fixes between iterations, re-run until graduated, take next batch. "It's not a benchmark until we run it all at once" — the ONLY reported number ever comes from a final single-config full-300 run at the end. Patchwork start/stop is fine. RESEARCH-ZONE DOCTRINE: analysis may see everything (gold patches, hidden test asserts, all our attempts) — it's research, finding the rules for representations/prompts; exports ONLY general rules. ~/swe/research/ holds per-instance analysis records (research_ledger.py add <iid> <note>); RUNTIME NEVER READS IT (fence audited: grep -r swe/research ~/Code/LLMOS hits only research_ledger.py). Runtime leakage rules stay absolute; format_job caught reading applied test_patch from work tree (too-good signal = presumed leaked) — now reads base commit via git show HEAD:. CURRENT STATE: git ~/Code/LLMOS at 57a7f05, tags run-v3.1/run-v3.2. Harness now has: triage+probe two-stage (retries on empty), locked invariant probes in gate, verify-delta (same_failure_as_last), reproduce cap (3 rejected greens pauses tool, unlocks on patch/read_range), WORKSHEET (deterministic state object refreshed into goal msg every turn: trying-to-get/repro/probe/patches-tried/next-obligation), best-by-form TTS attempt selection (form_rank: selfv8+repro4+probe2+patch1), tail hygiene, atlas LOO + auto-learn, spec_env. Standalone tools awaiting v4 wiring: symmap.py (symbols/pseudo/schema — usage-derived object fields; schema of flask rule = subdomain,endpoint,rule,methods), format_job.py (rank label candidates: attribute-reads > base-commit test words > issue words; correctly ranks Subdomain over Domain with zero leakage), research_ledger.py, workshop.py (iteration driver, queues behind other jobs, graduates solves out of runs/workshop/failset.json). RUNNING NOW: rerun_failed4.py on pop-os (fix4 harness = v3.1, no worksheet): flask-5063 MISS (format case: prints "Domain", reads rule.subdomain, hidden tests want "Subdomain"/"Host"; awaiting lint/schema wiring), requests-3362 RESOLVED att1, xarray-5131 RESOLVED att1 (both were 0-for-2 the night before — reproduce cap + fixes working), pylint-7993 last, in flight. AFTER IT COMPLETES: prune failset (remove 3362, 5131), add ledger records for remaining failures, launch workshop.py 1 (worksheet debuts). Key context: v1 stitched best ~49.7% (best-of-N); leaderboard SOTA 60.33 (Claude 4 Sonnet), best 30B EntroPO+R2E 49.67; our benchmark is HARDER (agent builds own env from bare clone; leaderboard runs inside prebuilt containers). Paper written: outputs/verifying_against_the_issue.md (Medium-ready). Determinism doctrine: temp 1.0 = sampler; single flips are weather; tool results carry decisions not prose; reduce choice points not temperature. Three restarts taught: measure the prompt the model receives; success theater is the recurring disease (archiver, clip, silent else, probeless triage) — per-instance status fields catch it.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **When rerun_failed4 completes: prune failset to survivors, ledger-record pylint result, launch workshop.py 1 (worksheet live for first time)**
- **Wire for flask-5063 crack: schema line into worksheet (objects-in-play) + diff naming lint (label outputs after fields they display)**
- **Between iterations: analyze each failure via the three questions, make GENERAL fixes only, record in research ledger**
- **After current batch graduates: next batch = next 4 misses (continue v3 sequence: sklearn-25747 etc.)**
- **Mine research ledger periodically: distribution of failures across the three questions = where the next representation rule is buried**
- **END STATE: single-config full-300 clean run + full Docker audit (incl PASS_TO_PASS) = the only reported number**

### Critical Success Factors
- Fixes between iterations must be GENERAL (never instance-specific answers into runtime)
- Runtime never reads ~/swe/research/ (re-audit after any new tool)
- Any suspiciously-good signal is presumed leaked until provenance traced
- Kill by explicit PID, never self-matching pkill; heredocs over ssh get mangled -- write local + scp
- All traces preserved (archiver fixed ffda5c4); trace_diff.py compares versions; snapshots in ~/swe/traces_snapshots/

## 🎯 Session Continuity

**BREAKTHROUGH**: llmos session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: llmos
3. **Continue from**: WORKSHOP MODE — iterate failure batches until each cracks; full-300 deferred until workshop done
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly