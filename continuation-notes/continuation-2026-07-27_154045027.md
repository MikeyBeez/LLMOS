# 📋 Continuation Note - SWE-bench harness for small local LLMs (ornith) - 7/27/2026

## 🎯 Session Status Dashboard
**Project**: SWE-bench harness for small local LLMs (ornith)  
**Phase**: Grading-correctness fixes shipped (committed + pushed). False-GREEN premise retracted. Next: re-grade history and pick the next real lever.  
**Completion**: 85%  
**Status**: Healthy, nothing in flight, GPU free. Two harness bugs found, fixed, verified, committed (LLMOS 7e669f6 on main). Corrected: compare3 = 16/18 (89%), compare2 = 15/18 (83%).  
**Last Update**: 2026-07-27T15:40:45.022Z

### ✅ Major Achievements This Session
- **Found and fixed TWO grading bugs that turned correct patches into misses -- _django_label branch precedence, and docstrings resolving to django's abstract mixin classes. Committed + pushed as LLMOS 7e669f6.**
- **Proved all three instances ever scored PASS_TO_PASS-regressed (15498, 16255, 16379) were false negatives -- correct patches misgraded in every run since compare1. compare3 goes 13 -> 16/18 (89%).**
- **Root-caused why NEIGHBOR_INJECT never fired (baseline captured only inside reproduce(), which ornith skips before its first patch) and fixed it -- then measured it firing 0 times in 6 evaluations, establishing the lever is unnecessary rather than broken.**
- **Retracted the session's governing premise honestly: the broke-neighbor false-GREEN category was substantially a measurement artifact, not model failure.**
- **Caught and corrected my own false-negative conclusion (blast_radius 'never fired' was an artifact of 120-char log truncation, not evidence) before it propagated.**

## 📚 Context Bridge for Next Session

### Enhanced System State
INFRA (unchanged): pop = 192.168.12.232 via mcp__remote-devices__system__system_exec -> ssh bard@192.168.12.232. Harness /home/bard/Code/LLMOS (git, branch main, origin git@github.com:MikeyBeez/LLMOS.git). Runs /home/bard/swe. ornith llama-server 127.0.0.1:8080. Single GPU => serialize runs. Edits: whole-function rewrite or anchored insert, count-assert==1, .bak, py_compile; NEVER sed. Transfer via readable-python quoted heredoc, never base64.

HEADLINE: the harness was UNDER-COUNTING. Two grading bugs in test_runner.py turned correct patches into misses. Both fixed, suite 48 passed, committed 7e669f6 and pushed.
 BUG 1 (_django_label precedence): SWE-bench django P2P entries come in three shapes; one is the test DOCSTRING. A docstring resolver (_django_docstring_label) already existed and worked, but was unreachable because (a) the paren-capture accepted ANY parenthetical, so "A cached sitemap index can be rendered (#2713)." became the label "#2713", and (b) the pytest-path branch fired on any "/", so "...alternate/hreflang links..." became a bogus module. django then ERRORed on unimportable labels and the harness read those errors as PASS_TO_PASS regressions. FIX: paren-capture must match ^[A-Za-z_]\w*(\.[A-Za-z_]\w*)+$; path branch requires "::" or ("/" and no whitespace).
 BUG 2 (abstract defining class): a docstring lives on the class that DEFINES the method, which in django is often an abstract mixin (`class BaseCacheTests:`, no TestCase base) -> "TypeError: test_x() missing 1 required positional argument: 'self'" -> also scored as a regression. FIX: new _concrete_test_class + _class_bases resolve to a runnable subclass (transitive TestCase-ancestry check; candidates ranked plain-TestCase-first, then source order). On cache.tests this picks LocMemCacheTests over DBCacheTests (TransactionTestCase) and over memcached/redis variants needing external services. Returns the class unchanged when already runnable (old behavior preserved).

CORRECTED SCOREBOARD. Audited EVERY runs/*/*.json: exactly THREE instances were ever scored "PASS_TO_PASS regressed" — django-15498, django-16255, django-16379 — and they were misses in every run (compare1, baseline, compare2, compare3). All three now grade F2P=True P2P=True on their patched trees through the normal pipeline. compare3 13 -> 16/18 (89%); compare2 12 -> 15/18 (83%). The 10/18 baseline is undercounted by the same bug and is NOT a valid comparison point.
 CAVEAT, do not overstate: 15498 verified on its actual compare3 tree; 16255 and 16379 verified on the PROBE trees which overwrote the compare3 checkouts (same code + inject on, so equivalent — but inference). compare2/baseline-era trees are gone and cannot be re-verified.

BLAST RADIUS: 33 of 300 instances (all django; 33 of 114 django = 29%) have >=1 docstring-form P2P entry. The bug only bites when F2P ALREADY passed (grading runs P2P only when res["ok"]) — i.e. it silently converted SUCCESSES into misses, which is why only 3 of 33 surfaced. Exposure grows as the model improves.

NEIGHBOR_INJECT — FINAL VERDICT: mechanically fixed (pre-mutation baseline fallback in h_patch, swe_fix_tools.py, gated on the flag so off-path is unchanged) and now reachable, but UNPROVEN AND PROBABLY UNNECESSARY. Across the 2-instance probe it evaluated 6 times, FIRED 0 times — no regressions existed to detect. Leave it env-gated and OFF. KEEP the probe/FIRED/NO-FIRE logging in swe_agent_v2.py permanently: the silent no-fire is what hid all of this for a GPU-night.

RETRACTED — do not cite again: "~75% false-GREEN", "declared-wrong 9 : never-converged 2", "7 of 9 BROKE A NEIGHBOR". Only 3 instances were ever P2P-regressed and all 3 were harness fault. That decomposition did not come from score_tail and must be re-derived from scratch if wanted.

### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives
- **Re-grade history properly: historical work trees are OVERWRITTEN, so past runs cannot be re-scored from artifacts. The only sound path is a fresh 18-instance run on current code with grading fixed -- that becomes the new, trustworthy baseline. Budget ~5-7h GPU (compare2 took ~19:31->22:5x for 18).**
- **Audit the other 30 of the 33 docstring-form-P2P instances: they are only exposed once F2P passes, so confirm the resolver picks a sane concrete class for each (offline, no GPU -- just call test_runner._django_label against each work tree that exists).**
- **Pick the next real lever now that broke-neighbor is void. The remaining known failure mode is the didn't-fix false-GREEN (weak reproduction); _reproduction_strength exists but is advisory only. Design a pre-submit repro-strength gate offline.**
- **Consider whether the same docstring/label class of bug affects FAIL_TO_PASS resolution on other repos (the audit found docstring-form entries ONLY in django, 33/114 -- astropy/sympy/etc were clean).**
- **DEFERRED, values call, keep env-gated OFF for measurement: web-searching the reproduction error retrieves gold fixes on these HISTORICAL instances and inflates the benchmark.**

### Critical Success Factors
- Only canonical hidden tests decide pass/fail; runtime NEVER reads gold/FAIL_TO_PASS during a run. Reading the answer key for POST-HOC diagnosis (as done today) is fine and is not a leak.
- pop is ALWAYS 192.168.12.232. Single GPU -- never launch a run concurrent with another.
- A running python process does NOT pick up harness edits (test_runner was imported at process start) -- 16379's in-run grading used the PRE-fix resolver. Re-grade after any grading change; never trust a score computed by a process that started before the fix.
- When two measurements disagree, do not assume the newer one is broken -- today the in-run detector was right and the GRADER was wrong. Read the primary artifact (score_logs/*.p2p.log), not the summary field.
- Summary fields lie: 'blast_radius absent from the log' was a 120-char truncation artifact, and given_tests_regressed=[] came from the same detector that gave a false all-clear.
- All harness changes env-gated where behavioral + count-asserted + .bak + py_compile; flags off must stay byte-identical. Grading fixes are necessarily always-on -- they change historical numbers by design.
- Per pref-always-update-git-no-asking: commit and push without being asked (done: 7e669f6).

### Testing/Validation Criteria
- [ ] test_runner._django_label resolves all four shapes: 'name (mod.Class)', pytest path, prose docstring with '/', prose docstring with a parenthetical -- verified
- [ ] _concrete_test_class returns a runnable subclass for abstract mixins and leaves already-runnable classes unchanged -- verified on cache.tests -> LocMemCacheTests
- [ ] 15498, 16255, 16379 all grade F2P=True P2P=True through the normal pipeline -- verified
- [ ] suite green (48 passed) after each change -- verified
- [ ] NEIGHBOR_INJECT off leaves behavior byte-identical (all new code inside the flag branch) -- by construction
- [ ] OPEN: a fresh 18-instance run on fixed code to establish a trustworthy baseline

## 🎯 Session Continuity

**BREAKTHROUGH**: SWE-bench harness for small local LLMs (ornith) session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: SWE-bench harness for small local LLMs (ornith)
3. **Continue from**: Grading-correctness fixes shipped (committed + pushed). False-GREEN premise retracted. Next: re-grade history and pick the next real lever.
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly