# 📋 Continuation Note - llmos - 7/20/2026

## 🎯 Session Status Dashboard
**Project**: llmos  
**Phase**: WORKSHOP MODE — mine general rules from the hard tail. Method changed today: HAND-SOLVE the instance first, then build the map the solve revealed.  
**Completion**: 70%  
**Status**: v5.8 (HEAD 9680613). 8 graduations, 3 parked. Iteration 14 running on sympy printing cluster.  
**Last Update**: 2026-07-21T01:01:56.722Z

### ✅ Major Achievements This Session


## 📚 Context Bridge for Next Session

### Enhanced System State
THE BIG IDEA THAT EMERGED TODAY (Mikey's framing, and it reorganises everything): A MAP IS A TRANSLATION LAYER BETWEEN ABSTRACTIONS. Bugs live at one level; the agent reads at another; between them sits a crossing nothing translates. You need one map per BOUNDARY, not per subsystem — and there are only about five. For each boundary: either supply the translation, or ELIMINATE THE CROSSING (cheaper and more reliable). Boundaries found so far: issue-vocabulary <-> codebase-vocabulary (flask-5063 parked, flask-4992 missed); source <-> runtime data movement (flow map); runtime object <-> rendered output (sympy printing, 23 instances); intended edit <-> argument encoding (SOLVED by DELETING the crossing — line-anchored patch); symptom <-> file location (atlas). A map exposes THE VARIABLES THE CODE BRANCHES ON, using the subsystem's OWN vocabulary (sympy's `baseline`/`binding`, python's `sys.modules[key]`, flask's `rule.subdomain`).

THE METHOD CHANGE — do this from now on: BEFORE working an instance, SOLVE IT BY HAND. Then notice what information was decisive; THAT is the map to build. Deriving maps from theory produced one that showed the symptom (what the output looks like) instead of the cause (whether the code can even place a block). Two hand-solves, two completely different outcomes, both valuable: 23191 -> solvable, yielded a new general map. 21171 -> not solvable from the repo, yielded a KB entry + a caveat on an existing rule + a park decision. LEAKAGE GUARD: the solve tells you WHICH general map to build, never what the map contains. Test: could this map have been built without seeing the answer? compose_map.py runs on any function in any repo and has no idea 23191 exists.

MY OWN FAILURE MODE, measured: SIX times today I asserted a field/function name from memory and was wrong — h_submit being on the dispatch path (it is not; submit maps to RETURN and the handler never runs), _run returning a dict (it returns CompletedProcess), _clip_result (never existed), events carrying result/exit (they carry `ok`), "we never ran a full pass" (results_full300.json: 161 instances, 40.4%), inst["patch"] (the field is `gold_patch`). EVERY ONE was caught by running something. NONE by thinking harder. Symbolic tools (Grep, a script that reads the file) were right first time, every time. Countermeasure now in the repo: schema.py — the canonical space for all four data shapes, with sget() that raises with a did-you-mean instead of returning None.

MEASUREMENTS THAT SHOULD SURVIVE (all reproducible from ~/swe/research/postmortem/, n=175): text anchors containing a backslash fail 77% vs 19% without — the agent OVER-ESCAPES when re-emitting escape sequences; "old_snippet not found" is the single most common error in the system (259 occurrences). The agent's self-authored reproduction is 30% false-positive AND 33% false-negative; base resolve rate 63%, given-green 70%, given-red 54% — i.e. ~7 points of information, near-useless, and form_rank was weighting it 14 of 15 points. 36 of 104 wins ended by EXHAUSTING THE BUDGET rather than declaring done — a third of our wins never knew they had finished. Fix-shape distribution by GOLD PATCH (not by report vocabulary, which only measures symptoms): signature/API-lag 80% fail (worst), output-format-string 68%, substantial-rewrite 63%, condition/branch 54%, import/registration 45%. sympy is 47 of 115 eligible hard instances; 23 of its 48 never-resolved are printing/rendering — same fail rate as other categories, just far more volume.</context_bridge>

<major_achievements>["8 graduations (was 3 this morning): +pytest-11148, django-17051, seaborn-3190, astropy-14995, django-17087. NOTE the honest split: only 4 were genuinely cracked (every prior run missed); 4 were FLAKY instances ([True,True,False] history) that resolved on first contact and taught us nothing", "LINE-ANCHORED PATCHING (v4.10) — the biggest single win. Text anchors with backslashes failed 66-77%; start_line/end_line cannot be mis-encoded. xarray-5131 went 2528s/15 patches/15 failures -> 453s/7 patches/4 failures, 5.6x faster, and the model reached for start_line unprompted", "AUTOMATIC POST-MORTEM on every run (v4.12) + backfilled 175 archived runs. Before this, 144 misses had produced 11 research records — 92% of failures never examined. This is what surfaced the escape signal that had been sitting in 25 separate failures for over a week", "GIVEN-TESTS-ONLY DECISIONS (v5.0, Mikey's ruling): score() now runs PASS_TO_PASS as well as FAIL_TO_PASS (we were running 1 of 7 given tests at the median); form_rank ranks on repo base-commit tests + syntax + real diff, with self-authored checks demoted to fractional tie-breakers. Three states not two: pass +8 / unknown 0 / REGRESSED -8", "compose_map.py — static AST map that classifies whether layout code composes BLOCKS or splices STRINGS. Correctly flags the broken printer and clears the healthy ones. Found by hand-solving 23191", "schema.py — canonical space for instance/trace/event/outcome/fix_state. All four validate clean against real data; catches every magic-string error made today", "v5.5 network resilience: a mid-run drop was recorded as an env failure (false miss). Network-shaped install failures now wait and retry once; genuine build errors still fail fast. A 5.5-day full run has ~1-in-3 odds of hitting a drop", "Patterns 42-44: always import time + NEVER let a helper fail silently; when you emit a SPEC-GOVERNED ARTIFACT the specification decides (not the surrounding code); follow the language's published style guide"]</major_achievements>
<next_actions>["Let iteration 14 finish (sympy printing: 23191, 21171, 20639, 21612). It loaded before v5.3-v5.8 so it tests NONE of them — the layout KB, the AMS standards rule and the patterns all arrived after it started", "HAND-SOLVE 20639 next (pi**(1/E) renders as a radical). _print_nth_root came back weak-STRING-SURGERY — it is the test of whether compose_map generalises beyond one instance", "Re-run 21171 with the new AMS delimiter rule in the KB. Clean test: 9 prior misses, and the rule is exactly what it lacked. If it converts, external-standards KB entries are a proven lever", "Wire compose_map into the patch step for printing subsystems — but ONLY after 20639 confirms it generalises", "Build the triage-report VALIDATOR properly: Mikey's point is that an inaccurate report must be REDONE, not annotated. Check the code-shaped names in triage against symmap; if they do not exist, retry triage once with the bad names fed back. The edit was written but never landed (the router dropped mid-apply)", "Park flask-4992? NO — v5.4 (naming rule now covers PROPOSED APIs) is the deduction it was missing. Re-run it", "Eventually: re-run the SAME 161 instances from results_full300.json on current code. That is the only comparison that says whether any of this moved the number from 40.4%"]</next_actions>
<critical_factors>["A 5.5-day full run is the only reportable number; workshop results are NOT a rate — the 13 instances touched are the hard tail, selected because they failed, and the scaffold changed ~30 times between instance 1 and instance 13", "HOLDOUT BUDGET is the real stopping criterion: we have derived rules from 13 of 300 instances (4.3%). At 20% the final number stops being interpretable. Proposal on the table: designate 150 instances the workshop never touches, report full-300 AND holdout-only; the gap IS the overfit, measured rather than argued", "Externally-cited KB rules (AMS docs, PEPs, RFCs) are the SAFEST content: provenance is public and verifiably not derived from a hidden test", "'Match the neighbours' has a failure mode: it fixed flask-4992 and points the WRONG WAY on 21171, where siblings never exercised the breaking case. Local convention wins on naming/structure; published spec wins on format correctness", "Restart the run after ANY code change — a live process holds a snapshot from launch and tests nothing. Check process start time vs file mtime before believing a result", "pgrep -f 'python3 workshop.py' MATCHES THE SSH COMMAND ITSELF. Use the bracket form: pgrep -f '[p]ython3 workshop.py'. This produced a false 'workshop is running' and cost an hour", "nohup > logfile does NOT truncate for a process that still holds the fd — a killed run's buffer restored stale content and I drew a dead-code conclusion from a corrupted log. Use a fresh filename", "Work dirs are frequently DIRTY from previous runs. git status before reading any file as if it were the base commit", "Write scripts locally and scp; heredocs over ssh mangle quoting (hit 3+ times today)"]</critical_factors>
</invoke>


### Session Continuity Enhancements
Context preserved for seamless handoff

## 🚀 Next Actions

### Immediate Objectives


### Critical Success Factors


## 🎯 Session Continuity

**BREAKTHROUGH**: llmos session continuity established!

### Expected Behavior on Restart
When you restart Claude Desktop, the system should:
1. **Automatically read** this continuation note
2. **Load project context**: llmos
3. **Continue from**: WORKSHOP MODE — mine general rules from the hard tail. Method changed today: HAND-SOLVE the instance first, then build the map the solve revealed.
4. **Maintain context**: Complete session continuity



---

**🔄 Next Action**: Restart Claude Desktop and validate session continuity
**Critical Test**: Should automatically load project context and process this note seamlessly